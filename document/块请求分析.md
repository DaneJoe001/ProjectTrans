文件列表 传输状态（是否已添加到下载队列，如何从下载队列中剔除）
停止某一任务（将该任务的id(应该由路径和名字确定)，在块队列pop时进行过滤，)
移除任务才在fileinfo表和blockinfo表中删除

点击开始任务->读取对应未完成的文件块列表，pop块信息到线程中进行请求，线程中读取块数据，写入文件，并更新块信息数据库

进度更新 块完成-》发送更新信号

先不考虑同时下载多个任务

不同路径不同名但同一文件

暂时对于同一链接不添加下载，